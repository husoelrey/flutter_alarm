<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 🔄 LAUNCH THEME (Flutter başlangıç ekranı için) -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- 🔄 NORMAL THEME (Flutter tarafında kullanılan) -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- 🔧 ESKİ AlarmRingActivity teması (kalsın ama kullanılmıyor olacak) -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@android:color/holo_blue_light</item>
        <item name="colorPrimaryDark">@android:color/holo_blue_dark</item>
        <item name="colorAccent">@android:color/holo_orange_light</item>
    </style>

    <!-- 🌌 YENİ: AlarmRingActivity için tam ekran, lacivert-teal teması -->
    <style name="Theme.Alarm.FullScreen" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Tam ekran görünüm -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- Renk paleti -->
        <item name="android:windowBackground">@color/navy_bg</item>
        <item name="colorPrimary">@color/teal_accent</item>
        <item name="colorSecondary">@color/teal_accent</item>

        <!-- Sistem koyu tema devre dışı (isteğe bağlı) -->
        <item name="android:forceDarkAllowed">false</item>
    </style>

</resources>
